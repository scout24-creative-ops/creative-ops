# Documentation — 2026-W38

_Period: 2026-09-14 to 2026-09-20_

## Summary status

- Last summary created: none
- Summary scope: No completed summary yet

## Updates since last summary

- 2026-09-14 — Landing Page Builder: Validated the full Contentful Action lifecycle on a disposable LP Builder entry. Read by slug and entryId, draft creation/update, publish, unpublish, archive, unarchive and delete all worked end to end.
- 2026-09-14 — Landing Page Builder: An intermittent GPT Action authentication failure remains a reliability blocker. Mukhammadjon reproduced the same issue and confirmed that the specific Authorization-header error does not come from the Contentful Lambda auth check. The current working assumption is an upstream ChatGPT OAuth/session issue, so no GPT/OAuth configuration change is planned yet.
- 2026-09-15 — Landing Page Builder: Completed the visual Contentful Module Library cleanup. `libraries/module-library.html` is the single visual library source, with placeholder imagery, working Carousel, Video, Checkmark List, Accordion and Sticky previews, plus library-only copy controls and spacing kept separate from productive module rules.
- 2026-09-15 — Landing Page Builder: Synchronized genuine visual-library module changes back into the GPT package and audited the result independently. Productive Hero, Teaser, Content Cards, Video, Carousel, Accordion and Sticky Footer definitions now match the intended current versions while library-only helpers remain excluded.
- 2026-09-15 — Landing Page Builder: Validated the COSMA icon basis against the current Contentful host. The host uses `@is24/cosma-ui-icons` 6.24.0 and the maintained static inventory contains the same 899 icon classes. GPT guidance now uses validated COSMA/S24 icon classes as the default and keeps existing deliberate SVG/chevron exceptions.
- 2026-09-15 — Landing Page Builder: Published the remaining productive Bridge/Runtime work for Card Carousel, Checkmark List, mobile stacked-grid spacing, Accordion ownership and Sticky Footer behavior. Runtime tests are 13/13 and the final real Contentful smoke test confirmed Carousel, Accordion and Sticky Footer behavior.
- 2026-09-15 — Landing Page Builder: The Accordion issue was traced to duplicate interaction ownership. The Contentful renderer already owns Accordion behavior, while the LP Builder runtime had added a second handler. The runtime Accordion initializer was removed so the renderer is now the single owner; no page HTML change was required.
- 2026-09-15 — Landing Page Builder: Confirmed a renderer integration gap for the shared Bridge stylesheet. The Contentful frontend loads the global LP Builder runtime but does not currently render the Bridge stylesheet, even though the Bridge URL exists in frontend code. Older working LP pages still carried a page-level Bridge link in their stored HTML. A controlled smoke test that added the current Bridge link directly to the test page restored correct styling immediately, proving the maintained module HTML and Bridge work together. The durable solution is central Bridge loading by the Contentful renderer and requires Core Frontend follow-up.
- 2026-09-15 — Landing Page Builder: Audited the old AEM module editability/guardrail rules against the current Contentful GPT package. Contentful already protects technical structure well, but often does not explicitly state which fields a user may change, remove or swap. Of 35 checked module mappings, 6 were fully covered, 16 partial, 1 missing, 4 intentional differences and 8 required verification. The main design direction is a two-level editability model: global rules for copy, links, CTA/media/structure/surface/icon/item-count behavior plus module-specific exceptions in `module-contracts.md`. No Contentful rules were changed yet; the audit is the basis for a later governance pass.
- 2026-09-16 — E-Mail Automation – Plus FOMO: Built a new HTML email concept for the deactivated-listing / Bewerbungsassistent follow-up scenario using the existing local email project and reusable email patterns rather than a separate implementation. Two structural variants were prepared: V1 without and V2 with an additional `Ähnliche Angebote ansehen` CTA below the deactivated property card. The mail uses dynamic property/name/image placeholders, the existing footer/app-download pattern and Outlook/VML-safe buttons. Rendering-tool QA passed after correcting the headline wording, using the established dark-mode-safe ImmoScout24 logo, and testing the assistant content box in three color variants: blue `#00DFFF`, yellow `#F5F200` and grey `#F5F5F5`. All comparison variants now use the same Charcoal `#333333` primary CTA so only the background color changes. Six templates plus six matching review files were prepared and sent to a colleague for comparison/review.
- 2026-09-16 — AI Enablement Series: Confirmed a format change for the recurring sessions based on the first 90-minute meeting and feedback from Team Leads. The session on 2026-09-08 showed that attention dropped noticeably after roughly one hour, so the recurring format is shortened to 60 minutes starting with the next session on 2026-09-22. Future agendas should therefore use fewer, more clearly timeboxed blocks and should not assume that all three pillars need to fit into every session.
- 2026-09-16 — AI Enablement Series: Started planning the 2026-09-22 session. `Learn` is intended to focus on ChatGPT Skills: what a Skill is, benefits, how to build and configure one, how Skills differ from Agents, and a live Skill build. The earlier idea of building a Skill mainly to write information into Scout Wiki is no longer considered necessary and would have been too much for the first session. `Show & Share` is planned with Ulrike sharing takeaways from an external AI event; details are still pending. `Explore` may revisit the Single-Source-of-Truth / central knowledge-source topic from the first session, but its relevance for the full participant group is still being assessed and is not yet confirmed for the agenda.
- 2026-09-16 — AI Enablement Series: Clarified that the Creative Hub / shared asset-library work is a separate initiative and should not be used as evidence for the SSOT Explore topic. The SSOT discussion is about reusable knowledge/data sources for AI use cases. Dominik recalls that someone in the 2026-09-08 session may have suggested checking how the portal AI assistant `HeyImmo` sources and maintains its knowledge; the transcript does not preserve this clearly enough to treat it as confirmed. Asking the HeyImmo team how they handle canonical sources, freshness and ownership is a possible research step, not yet a confirmed task.
- 2026-09-16 — Working preference: For prompts handed off to Codex or another execution tool, always show a short recommendation block immediately before the prompt with the target tool, recommended GPT model, reasoning intensity and whether any files should be uploaded. Current VS Code Codex model choices shown by Dominik are Standard, GPT-6 Astra, GPT-5.6 Sol, GPT-5.6 Terra, GPT-5.6 Luna and GPT-5.5. For GPT-5.6 Sol, the shown reasoning choices include Mittel, Hoch, Sehr hoch and Ultra. Model/reasoning should be chosen per task rather than using maximum effort by default.
- 2026-09-16 — Landing Page Builder: Investigated missing spacers in Contentful GPT-generated pages. The technical spacer system was intact: `foundation-rules.md` already defines the canonical rules and the productive Bridge supports the full spacer set. Root cause was a GPT Instructions/packaging regression: the mandatory spacer sequence had been moved out of the main Instructions and only indirectly referenced, so the GPT could compose modules without reliably applying Foundation rules. The active local GPT package was corrected by making the Foundation spacer invariant explicit again in `main-instructions.md` and adding a final validation for missing, duplicate, consecutive or opening spacers. A regression test was added; `node --test tests/*.cjs` passed 15/15 and `git diff --check` passed. The GPT Builder still needs the updated `main-instructions.md` to be applied manually.
- 2026-09-16 — Landing Page Builder: Correction to the spacer investigation follow-up. The reported `module-contracts.md` "ACTIVE whitelist" inconsistency is stale/invalid context: the ACTIVE whitelist had already been removed in earlier LP Builder work. Do not treat the old whitelist wording as a current architecture rule or create follow-up work to reconcile library modules against it. If the local working copy still contains that wording, first verify whether Codex inspected an outdated or unsynchronized file before changing anything.
- 2026-09-16 — Landing Page Builder: Updated the GPT output convention so Action-returned preview URLs are presented as a Markdown text link `Preview: [Open Preview](...)` instead of exposing the raw URL as visible link text. The technical Action contract remains unchanged, debugging/error URLs may still be shown in full, and the new regression coverage passes with `node --test tests/*.cjs` at 16/16 plus `git diff --check`. The updated `main-instructions.md` still needs to be applied manually in the GPT Builder.
- 2026-09-16 — Landing Page Builder: Started a reusable page-template direction for the Contentful GPT. The first planned template is `Blueprint`, defined as a module composition rather than copied page HTML. Initial module sequence: `hero-split`, `steps-3col`, `teaser-split-image-right`, `teaser-split-image-left`, `counter-animated`, `accordion`. Existing Foundation spacing rules should continue to insert spacers; templates should not duplicate spacing logic. The structure should be extensible so additional named page templates can be added later.
- 2026-09-17 — Landing Page Builder: Established a clean current Custom GPT baseline after the package rebuild and passed a reusable three-prompt acceptance suite without retries. The run covered Blueprint draft creation, a single full-HTML edit combining text, CTA label/link/variant changes, module add/replace/reorder, slug rename, Main Title update and spacer recalculation, followed by publish, unpublish, archive, unarchive, republish, reunpublish and delete. Component Library retrieval, explicit-spacing/root invariants and renderer-owned Bridge/Runtime exclusion all passed, with no Action, JSON, serialization, auth or validation error. The reusable suite is now documented in the Profile repository for future GPT package changes.
- 2026-09-17 — Landing Page Builder: Confirmed the current GPT package architecture after the rebuild. `main-instructions.md` is the single Instructions file; Knowledge consists of `component-library.html`, `building-policy.md`, `contentful-integration.md`, `source-duplicate-mode.md` and `cosma-icons-static.md`. The Component Library is the capability SSOT, the Building Policy owns composition/editing/spacing, and Mukhammadjon's OpenAPI schema remains the unchanged read-only Action SSOT. The current Component Library exposes 33 Page Modules and 12 Embedded Component groups; there is no ACTIVE/whitelist state. Pending module-specific policies do not block module use.
- 2026-09-17 — Landing Page Builder: Mukhammadjon's central Bridge CSS fix is now reflected in the working baseline, and the complete regression run did not reproduce the earlier Action/auth failures. The current GPT setup is therefore treated as green and should remain frozen unless a concrete requirement or regression appears. Large `getLpBuilderPage` read-back at roughly 50 KB remains a separate explicit retest because the regression entry was smaller.
- 2026-09-17 — Contentful Migration: Audited the historical crawl work across the existing migration and LP Builder workspaces before defining the next reusable approach. The audit found useful reusable pieces for URL discovery, page/content extraction, browser capture, screenshots, stable block IDs, asset download/hash/deduplication and migration packages, but no evidence of one complete standardized crawl of the full B2B directory. The 369-URL candidate file is scope input, not proof that all 369 pages were crawled.
- 2026-09-17 — Contentful Migration: Defined a reusable `Migration Crawler` as the practical working-student project. It is not a GPT Agent, ChatGPT Skill or dedicated UI; it is a small reusable crawler/workspace that Codex can execute for a URL list or AEM directory. The crawler should preserve LP Builder-relevant source evidence such as content order, text, links, interactions, rendered DOM, desktop/tablet/mobile screenshots and capture gaps, and produce a structured package that can later be interpreted by the current LP Builder.
- 2026-09-17 — Contentful Migration: Clarified the asset-migration contract for the crawler. Each relevant AEM asset should be downloaded/captured, linked to its original AEM URL, assigned a stable technical ID and content hash, and recorded in an asset manifest. The target mapping is `AEM URL -> Asset ID -> later S3 URL`; the ID is the migration identity, while final Contentful HTML should use the new persistent S3/delivery URL. Existing ALT text should be retained; missing ALT text should be generated automatically from image and page context, with manual review only for ambiguous cases. S3 upload itself remains a later controlled phase once the storage setup is confirmed.
- 2026-09-17 — Contentful Migration: Created and iterated a concise bilingual Scout Wiki page `Migration Crawler – Briefing` under Creative Operations / Contentful Migration. The final briefing explains the project in simple terms, separates LP Builder crawling from S3 asset preparation, and is intended as the working-student handoff.
- 2026-09-17 — Landing Page Builder / Contentful Migration: Implemented the unified GPT Migration Mode in the local LP Builder package. `migration-mode.md` replaces `source-duplicate-mode.md` and defines exactly `LOCKED_IMPORT` for exact supplied HTML and `CRAWL_REBUILD` for one or more crawler-driven source pages. Crawl Rebuild uses Component Library + Building Policy, preserves source content/links, reports per-page statuses and represents unsupported areas through a migration-only placeholder built from existing `callout--base` markup plus `data-lpb-migration-*` markers. No new Component Library capability, Bridge CSS, Runtime or Action was introduced. Mukhammadjon's OpenAPI SSOT remains unchanged and both schema copies still match by SHA-256. Local package/runtime tests pass 50/50; the changed package still needs real Custom GPT acceptance before becoming the new green baseline.
- 2026-09-17 — Contentful Migration / Marketing Asset Library: Aligned the S3/image-delivery direction with John Ford. John recommended exporting original AEM assets to S3, preserving the existing `/content`-style structure where practical, because crawled page renditions may already be resized or optimized. The crawler should therefore preserve AEM asset references and support mapping them back to original source files rather than treating crawled image downloads as the canonical master. John's existing image-scaler can produce production-ready delivery variants from S3 assets; advanced cropping is possible but explicitly outside the MVP. The first target is simply original asset -> S3 -> scaler URL -> LP Builder.
- 2026-09-17 — Contentful Migration / Marketing Asset Library: Bea identified the existing `is24-cms` AWS account as a possible home for the S3 pilot (alias `is24-cms`, HoT Daniel Herold). The earlier idea of requesting a new `marketing-assets` account is paused for now. John has been asked whether the CMS account works for the bucket/scaler setup and what permissions are required. Longer term, the direction remains a cross-Marketing Asset Library that can serve Contentful, Beefree, Iterable, ChatGPT agents and other tools; long-term account/cost ownership will be decided only after the MVP proves the approach.
- 2026-09-17 — Marketing Asset Library: John confirmed that the existing `is24-cms` AWS account is sufficient for the pilot. He recommends creating the S3 bucket through CloudFormation rather than manually through the AWS web interface and asked whether Dominik has GitHub. Dominik confirmed GitHub access and existing use for LP Builder-related files. No repository or infrastructure path will be created pre-emptively; the topic is intentionally paused until John provides the next technical setup instruction.
- 2026-09-18 — Marketing Asset Library / Contentful Migration: Reconfirmed from the John Ford alignment that the migration should use original upstream AEM asset files rather than only crawler-visible renditions. John recommended downloading the relevant AEM images and preserving their existing `/content/...` path/folder structure so crawled page references can later be correlated back to the original source files before upload to S3 and scaler delivery. An Excel/URL inventory can support metadata and mapping but does not replace the original files. Dominik contacted Stefan to clarify whether the relevant B2B/AEM assets can be exported in this form, ideally with paths and available metadata such as ALT text; response pending.
- 2026-09-18 — Marketing Asset Library / Contentful Migration: Stefan confirmed that a bulk asset extraction for the B2B migration is feasible and had already briefly discussed the topic with Bea. His proposed approach is a dedicated crawler that receives a list of AEM page URLs, downloads all `img src` targets and optionally file-linked `a href` targets for a configurable set of document extensions, and uses the AEM asset path as the unique filename/key. He noted that the first collection will likely also contain duplicates and non-content assets such as icons, with cleanup/deduplication to happen after capture. Important technical point still to clarify: John's requirement is to preserve original upstream AEM files, while a page crawler may only retrieve rendered/rendition URLs; the crawler therefore needs either original-DAM resolution or a reliable mapping back to the canonical AEM source asset.
- 2026-09-18 — Contentful Migration / Marketing Asset Library: Stefan created Linear ticket `LCMS-7518` (`Build crawler to discover image and document assets from page URL lists`) for the AEM asset-extraction part. The scoped crawler accepts page-URL lists, collects `img[src]` plus configurable file links from `a[href]`, resolves relative URLs, deduplicates and downloads assets while preserving the AEM-discovered path. Stefan estimated the implementation at roughly one to two days once capacity is available.
- 2026-09-18 — Contentful Migration / Marketing Asset Library: Stefan clarified that most relevant AEM images are stored with the page rather than in DAM and therefore have no renditions. For DAM-backed images, he confirmed that the original asset URL can be derived from a rendition URL and updated `LCMS-7518` accordingly, resolving the main original-quality concern from John Ford.
- 2026-09-18 — Contentful Migration: Beatrice confirmed that Jonas had already been asked to look at Dominik's `Migration Crawler – Briefing`; Jonas found the topic interesting but is on vacation next week, so Bea will check whether there is anything useful to hand over. If no urgent AEM work intervenes, Maciej can take over after his current maintenance task. Dominik and Bea agreed to continue the coordination when Bea follows up next week. Until then, no parallel crawler implementation should be started; temporary AEM asset URLs remain an acceptable migration-draft fallback until the durable asset pipeline is ready.
- 2026-09-18 — Landing Page Builder / Contentful Migration: Tested `CRAWL_REBUILD` against `https://www.immobilienscout24.de/anbieten/gewerbliche-anbieter/lp/sichtbarkeit-verbessern.html` using a fresh crawl package. The GPT preserved the core copy and built some representable sections, but replaced most image/text sections with large `ASSET_MISSING` migration placeholders even though the underlying LP Builder structures were representable and usable AEM/source image URLs existed. The FAQ accordion was rebuilt successfully, while the contact form correctly remained a genuine `FORM_MISSING` gap.
- 2026-09-18 — Landing Page Builder / Contentful Migration: Confirmed the migration asset fallback direction: missing final S3/scaler delivery must not force a whole representable section into a placeholder. During migration drafts, the LP Builder may use the existing AEM/source asset URL temporarily and treat the asset as pending migration; real placeholders remain for genuinely unavailable assets or unsupported structures/forms. Before final AEM-independent delivery, known temporary asset URLs should be rewritten in bulk from a centrally generated asset manifest/mapping rather than by manually editing each Contentful page.
- 2026-09-18 — Contentful Migration: Clarified the operating split with Ulrike. Ulrike now owns the URL-side migration preparation: review which AEM URLs should or should not be migrated, keep those decisions in the Scout Wiki migration list, and identify remaining URL-scope gaps. That Wiki list is the operational basis for subsequent LP Builder migration work. As a result, Dominik no longer owns separate tasks for Handbook QA handoff, additional `/lp` scope review, Sitemap V2 preparation or the Gewerbliche-Anbieter start-page final check.
- 2026-09-18 — Landing Page Builder: Refocused the remaining migration-readiness backlog. Dominik still needs to add freely placeable headline and body-text capability as an explicit Builder rule and represent it visually in the Component Library; resolve the remaining module-specific Building Policy decisions; and build the previously investigated SEO/anchor-navigation module with sticky sidebar. Whether the SEO module is needed immediately for the migration remains open and should be clarified by Ulrike from the URL/page scope.
- 2026-09-18 — New support request / Editorial Lighthouse: Natalyi from Matthias Brandstäter's area approached Dominik after a recommendation from Matthias and asked whether he could support a Lighthouse initiative. The exact ask is not yet clear. The shared `Scaling Editorial at IS24` deck describes a 12-month editorial growth pilot and, in its agentic operating model, an orchestrated production chain with Trend Finder, Story Generator, Research/Writer and Image Creator agents plus human editorial/SEO oversight. Treat this as a provisional support request until Natalyi clarifies the Lighthouse scope, desired outcome and Dominik's role; do not create a separate project/task yet.
- 2026-09-18 — Editorial Lighthouse support assessment: Based on Natalyi's deck and Dominik's current working model, the likely useful role would be workflow/solution ownership rather than owning every production-infrastructure detail: break the editorial process into specialized agent steps, define clear inputs/outputs, source and quality guardrails, human-review gates and a first orchestrated prototype, then involve Engineering if the target requires production-grade scheduling, state, APIs, retries or observability. This is still an assessment, not a confirmed assignment; no separate project/task is created until Natalyi clarifies the Lighthouse scope and expected contribution.
- 2026-09-18 — Working preference / 1:1 with Jorin: Dominik is replacing recurring presentation decks with compact, visually presentable Scout Wiki pages for 1:1 preparation. Rationale: the Wiki is faster to maintain, remains useful as documentation, and avoids presentation-prep overhead when slides are not used in the meeting. A new private `1:1 Updates | Jorin` hub was created in the Creative Operations Scout Wiki space, with one dated/meeting-specific subpage per 1:1. The intended style is presentation-like rather than document-heavy: very large section headlines, short copy, strong visual grouping/columns, and only the few points needed for the conversation.
- 2026-09-18 — AI Enablement Series: Jorin had already received positive feedback from participants after the first regular session. The mixed-block format was perceived as varied, and it was important that colleagues contributed their own examples and learnings rather than Dominik presenting everything himself. At the same time, the 90-minute length was too long, so the recurring format is being reduced to 60 minutes. The next session is planned around three possible blocks: a practical Skills introduction with a live Skill build, Ulrike sharing takeaways from an AI conference, and—if preparation is sufficient—a continuation of the open Single-Source-of-Truth discussion for Marketing data and knowledge.
