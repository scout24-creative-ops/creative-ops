# Week 36 — 2026

## Summary status

No completed summary yet.

## Updates since last summary

### 2026-08-31 — Contentful-enabled LP Builder handed over and migration-focused duplicate established

Dominik completed the practical handover of the Contentful-enabled Landing Page Builder setup with Mukhammadjon. The duplicated GPT was configured with the required OAuth and GPT Action setup, and the full flow was successfully tested end to end: authentication, draft creation, preview, update, explicit publish and the resulting production URL. Dominik informed Mukhammadjon that the setup works and that he will continue adapting his own duplicate for migration use, while Mukhammadjon remains the relevant Core Frontend contact if the Action, renderer or environment contract changes.

Dominik also created a separate local workspace for the migration-focused Builder. The current working model is deliberately incremental: preserve the proven Contentful integration, but rebuild the product logic step by step instead of carrying over all behavior from the old Landing Page Builder. The first version is a controlled module composer rather than an autonomous migration agent: the user provides the intended module sequence and real content; the GPT uses approved module structures, preserves markup/classes, does not invent content and does not automatically select, reorder or substitute modules.

### 2026-08-31 — v0.1 module scope deliberately reduced to a whitelist

To make the rebuild easier to test and maintain, Dominik decided not to physically delete modules from the existing component library. Instead, the complete library remains available as reference while v0.1 uses a small active whitelist.

The active v0.1 modules are: `hero-split`, `teaser-2col`, `teaser-3col`, `benefits-2col`, `benefits-3col`, `accordion`, `callout--base` and `checkmark-list`.

This whitelist is intentionally provisional and is not meant to cover the final Anwenderhandbuch migration. Missing migration-specific modules will be added later from real page-group requirements. The point of the whitelist is to reduce the working surface while the new Contentful/COSMA rendering contract is established.

### 2026-09-01 — CoreCSS/COSMA-first direction verified for static LP Builder HTML

Dominik verified an important technical direction for the new Contentful Builder. The `is24-cms-frontend` globally loads `is24-corecss` 9.2.0, `is24-corecss-server` 9.2.0 and `@is24/cosma-ui-icons` 6.24.0 under the COSMA theme. Repository inspection confirmed that many CoreCSS classes are used directly on normal HTML elements rather than only through React components.

This materially simplifies the Builder architecture: static `htmlSource` can reuse the existing Scout24 design-system layer instead of recreating a separate CSS world. Verified native families include typography (`font-heading-*`, `font-body-*`), spacing (`margin-*`, `padding-*` variants), grid/responsive layout (`grid`, `grid-flex`, `grid-item`, `one-*`, `palm-*`, `lap-*`, `desk-*`, gutter variants), standard buttons (`button-primary`, `button-secondary`), several basic utilities and the COSMA icon font (`s24-icons-*`).

The current strategic rule is therefore **CoreCSS/COSMA first**. Own LP Builder CSS should only cover the real delta that static HTML cannot express through the globally available system, for example selected media behavior, surface/radius combinations or module-specific layout. Any such bridge should use existing COSMA design tokens rather than introduce a parallel design system.

The older LP Builder runtime styles remain necessary for existing AEM pages and must stay backward-compatible, but they should not become the styling basis of the new Contentful module library.

### 2026-09-01 — LP Builder renderer behavior and Accordion bridge clarified

The Contentful LP Builder renderer sanitizes and injects `htmlSource` as static HTML inside the React application. Static HTML is therefore not automatically converted into ordinary Contentful React components, but it does inherit the globally loaded CoreCSS/COSMA styles.

For Accordion there is already a dedicated LP Builder interaction bridge in the frontend. The renderer initializes HTML using `.accordion`, `.accordion__item`, `.accordion__trigger`, `.accordion__panel` and `.is-open`, and manages expanded state, ARIA attributes and panel height. This means the migration-focused Builder does not need to recreate Accordion JavaScript; only its final HTML/styling contract needs to be aligned.

### 2026-09-01 — Next technical proof defined: COSMA/CoreCSS HTML playground

The next planned implementation test is a dedicated COSMA/CoreCSS playground on the existing LP Builder smoke-test page `/dev-lp-builder-v01-test`.

The playground should intentionally avoid the old LP Builder runtime CSS and instead render representative native HTML examples for typography, spacing, two- and three-column responsive grid, buttons, utilities, icon font, raw media and the existing Accordion hook. The test should distinguish three outcomes: `USE NATIVE CORECSS`, `HTML STRUCTURE / HOOK REQUIRED`, and `LPBUILDER CSS BRIDGE REQUIRED`.

Only after this browser-level proof should the eight v0.1 modules be migrated toward the new CoreCSS/COSMA contract and any small LP Builder-specific CSS bridge be created.

### 2026-09-01 — Playground icon candidates tightened to verified font classes

Repository verification found no evidence for the provisional playground classes `s24-icons-home`, `s24-icons-search` or `s24-icons-heart`. The frontend tests explicitly verify `s24-icons-s24_magnifier_24` as a supported COSMA font-icon class and also use `s24-icons-apple_24` and `s24-icons-s24_user_24` in rendered components/tests. The playground should therefore avoid the unverified generic names and use only verified examples until browser rendering confirms additional icon classes.

### 2026-09-01 — Playground update blocked by missing GPT Actions in the active session

The prepared playground update could not be applied to `/dev-lp-builder-v01-test` because the active GPT session did not expose the Contentful actions required to read and update the existing draft (`getLpBuilderPage` / `updateLpBuilderDraft`). No new entry was created, no draft content was changed and nothing was published. The next retry should use a session in which the existing Contentful GPT Action is actually available, then read the page by slug, replace only `htmlSource`, save the draft and return the preview URL without publishing.

### 2026-09-01 — v0.1 component library refactored to CoreCSS/COSMA-first

The eight active v0.1 modules in the local Contentful LP Builder workspace were refactored onto the new CoreCSS/COSMA-first contract. The active blocks now use native typography, spacing, grid/responsive and standard button classes where confirmed. Legacy LP Builder-specific styling families such as `lp-*`, `ai-button`, `button-filled-*`, `button-outline-*`, old Hero/Teaser structure classes and Chevron asset markup were removed from the eight active modules. Accordion hooks, the Callout structure and the Checkmark bridge were deliberately retained where they still represent renderer or module-specific behavior. A compact `corecss-cosma-contract-v0.1.md` now documents native CoreCSS usage, hooks and remaining bridge requirements per module. The reference library remained unchanged. Open visual bridges are explicitly marked `OPEN / VERIFY` because `Scout24/is24-cms-frontend` is not available in the local Codex workspace. The next validation step is to load this refactored library into the Custom GPT `LP Builder – Contentful` and test a real Contentful preview there before publishing.

### 2026-09-01 — First real Contentful preview validates the CoreCSS/COSMA-first v0.1 baseline

Dominik loaded the refactored v0.1 library into `LP Builder – Contentful` and updated the existing `/dev-lp-builder-v01-test` draft without publishing. The preview successfully rendered `hero-split`, `teaser-2col`, `benefits-3col`, `callout--base`, `accordion` and `checkmark-list` using the new library. The desktop result confirms that native CoreCSS typography, spacing, grid/responsive layout and standard buttons are working as intended at a useful baseline quality. `teaser-2col` and `benefits-3col` visually validate the native grid approach especially clearly. Remaining validation is now narrow rather than architectural: the Hero media proportion/alignment needs review, `callout--base` is visually flat and remains a likely small surface bridge, Accordion interaction still needs an actual click/open-state test, and mobile responsive behavior should be verified. The existing Checkmark bridge renders successfully. No entry was newly created and the draft was not published.

### 2026-09-01 — Mobile baseline confirmed; Accordion gap narrowed to visual parity

Dominik confirmed that the same v0.1 test page also renders well on mobile, so the responsive CoreCSS approach is now validated in the real Contentful preview. Accordion interaction works correctly: items can be opened and closed through the existing LPBuilder renderer hook. The remaining Accordion issue is visual rather than behavioral: the trigger typography does not match the native Contentful Accordion and the right-side disclosure arrow is missing. The existing Contentful implementation in `is24-cms-frontend` uses the CoreCSS server Accordion component (`Accordion.Root`, `Accordion.Item`, `Accordion.Header`, `Accordion.Content`) rather than the raw LPBuilder HTML hook, so the native component is the correct visual reference for typography and disclosure affordance. The next Accordion task is to compare the native component output/docs against the raw LPBuilder hook and reproduce only the minimal verified visual delta, rather than replacing the working hook or adding new JavaScript.

### 2026-09-01 — Validation broadened from module fixes to a reusable design-system reference page

Dominik decided not to spend the next iteration on Accordion-specific visual detail. The higher-value next step is a comprehensive CoreCSS/COSMA reference page rendered through static LP Builder `htmlSource`, covering the available typography scale, colors/tokens, buttons including the newer `ButtonRounded` variants, spacing, borders/radii, grids/responsive behavior, icons and basic HTML primitives. The goal is to establish the full verified static-HTML design-system vocabulary before doing further module-level polish. Rounded buttons are explicitly an important verification target: the frontend uses `ButtonRounded` with variants including `filledDefault`, `floatingDefault`, `filledWeak`, `outlineStrong`, `textDefault`, `elevatedInversive`, `filledBrand`, `outlineWeak`, `textDanger`, `filledDanger`, `filledDangerWeak` and `filledSuccessWeak`, but their direct static-HTML contract still needs to be proven rather than assumed.

### 2026-09-01 — CoreCSS/COSMA reference page successfully validated in Contentful runtime

Dominik rendered the dedicated `corecss-cosma-playground.html` unchanged through the existing `/dev-lp-builder-v01-test` Contentful draft and confirmed the runtime result visually. The reference page shows the expected CoreCSS typography scale, COSMA text and background token colors, border and radius tokens, spacing tokens, classic buttons, grid layouts, basic utilities, static COSMA font icons, links, lists and raw media. This materially strengthens the static-HTML contract: the globally available CoreCSS/COSMA layer is not only present in source but visibly usable through LP Builder `htmlSource` across the major design-system primitives. `ButtonRounded` remains intentionally documented as component-only because no static HTML/CSS contract was found; it should not be approximated with invented classes. Accordion remains a renderer-hook case whose interaction can be tested separately from visual parity. The playground draft was not published.

### 2026-09-01 — ButtonRounded analysis points to a renderer-level primitive rather than static HTML copying

Using the newly connected GitHub MCP access in Codex, the current Contentful Call-to-Action implementation was traced end to end. The documentation page renders real Contentful sections through the normal React component resolver; Call-to-Action ultimately reaches `ButtonFactory`, which renders `ButtonRounded` from `is24-corecss-server`. The verified React contract includes twelve variants, `small`/`medium`/`large` sizes, `content`/`fixed` width behavior and link rendering through `as='a'`. The CTA adapter does not currently implement icons.

No stable static DOM or CSS-class contract for `ButtonRounded` could be verified because its final markup lives inside the CoreCSS server package rather than `is24-cms-frontend`. Copying internal markup into LP Builder `htmlSource` would therefore create an unsupported dependency on package internals. The technically preferred direction is a small semantic LP Builder renderer contract that can instantiate the native React `ButtonRounded`, analogous in principle to the existing Accordion renderer hook. A CSS bridge remains only a possible fallback and direct static DOM copying should be avoided. The exact semantic hook shape, icon behavior and any form-button requirements remain open design questions; no implementation was made.
