# Documentation — 2026-W37

_Period: 2026-09-07 to 2026-09-13_

## Summary status

- Last summary created: none
- Summary scope: No completed summary yet

## Updates since last summary

- 2026-09-07 — AI Enablement Series: Finalized the content and facilitation flow for the first recurring 90-minute session on 2026-09-08. `Learn` will cover Scout Wiki with a live flow from plugin connection and Wiki orientation to creating a fictional `ScoutConnect Pro` product knowledge page through ChatGPT and reusing that knowledge in a fresh chat for an email teaser.
- 2026-09-07 — AI Enablement Series: Finalized `Show & Share` with three colleague contributions: Marie-Lord Lumumba on the Brand Agent – Tone of Voice and Agent vs. Skill framing; Peter on Creative Hub and Plus Studio; and Sophie on her practical experience with AI presentation tools, including what she tested, the resulting quality and what she actually needs.
- 2026-09-07 — AI Enablement Series: Defined the `Explore` discussion around whether Marketing & Communications needs a central knowledge source at all, what information would be useful there, and what maintenance and ownership would require. The topic is intentionally exploratory rather than a decision to use Scout Wiki as the solution.
- 2026-09-07 — AI Enablement Series: Set up two complementary Scout Wiki pages in the `Creative Operations` space: an internal moderator/demo-flow page with the ordered Scout Wiki demo steps and ready-to-use prompts, and the participant-facing 08.09.2026 session page with the three-column `Learn / Show & Share / Explore` agenda plus collapsible areas for live notes, recording/resources and decisions/follow-ups.
- 2026-09-07 — AI Enablement Series: Tested Scout Wiki as a reusable ChatGPT knowledge source using the fictional `ScoutConnect Pro` product. A fresh ChatGPT conversation can retrieve the Wiki knowledge and generate channel-specific content such as an email teaser. Direct Scout Wiki access from the Contentful LP Builder remains optional and depends on additional technical integration; Mukhammadjon was asked to check read-only access only if it is quick to add.
- 2026-09-07 — AI Enablement Series: Finalized and posted the bilingual German/English Slack agenda for the 08.09 session. The approved recurring agenda format uses one `🎓 AI Enablement | ...` heading, a friendly introduction, compact bullets for `📚 Learn`, `🔍 Show & Share` and `💬 Explore`, German first and English below, with Explore wording kept open when no solution has been decided.
- 2026-09-07 — Writer / communication workflow: Updated the Writer skill calibration with the approved AI Enablement agenda format and the rule that recurring AI Enablement posts should be provided in both German and English with the same structure.
- 2026-09-07 — AI Enablement Series: Simplified the recurring preparation model further. Dominik decided not to prepare slide decks for the Series because the preparation effort is too high relative to the value. Future sessions should use only the Scout Wiki session page to show the agenda; Dominik facilitates the rest verbally. Colleagues who contribute decide themselves whether and how they want to present or demonstrate their topic. The format should deliberately stay lightweight so the recurring meeting does not consume disproportionate preparation time.
- 2026-09-07 — Marketing Asset Library / Contentful Migration: Dominik had already discussed the broader Marketing Asset Library idea with Matthias Brandstätter and Paul Befort. Both responded positively to the direction. This supports continuing the idea as an exploratory longer-term opportunity while keeping the immediate migration need focused on a small persistent asset-storage and stable-URL solution rather than treating a full Asset Library as already decided.
- 2026-09-07 — Marketing Asset Library / Contentful Migration: Prepared a draft Scout Wiki page for the upcoming alignment with John Ford: `Marketing Asset Library – John Ford Alignment`. The framing starts from the broader Marketing problem of fragmented tool-specific asset libraries across systems such as AEM, Contentful, Iterable, Salesforce and Beefree, with duplicate uploads and inconsistent format/compression rules. The desired direction is one shared source with stable URLs and potentially centralized delivery/transformation, while the meeting is explicitly intended to discover what Scout24 already provides and avoid prematurely deciding on a new platform. The Contentful migration remains a useful small pilot/forcing function rather than the sole justification for the idea.
- 2026-09-07 — Landing Page Builder: Mukhammadjon confirmed that all three bundled platform requirements are valid and should be implemented in the proposed direction: larger `htmlSource` capacity of at least 256 KB, ideally 512 KB, with lossless read-back and length/SHA verification; lifecycle support centered on stable `entryId` including slug/target-path changes, unpublish, archive and delete; and globally loaded `lpbuilder-bridge.css` plus trusted `lpbuilder-runtime.js` with `LPBuilderRuntime.init(renderedLpRoot)` after rendering. The remaining work is implementation and end-to-end validation rather than further architectural alignment. The separate Scout Wiki demo request is not a dependency for these changes and does not need to be rushed before the 2026-09-08 demo.
- 2026-09-07 — Contentful Migration: The next migration scope should include the B2B product detail pages linked from the Gold membership page at `/anbieten/gewerbliche-anbieter/inserieren/Mitgliedschaften/gold.html`. The source page contains a large set of product tiles across areas such as Vermarktung, Image, Akquise, weitere Bestandteile and Zusatzprodukte. Before rebuilding or migrating those product pages, Dominik wants a Codex crawl that creates a reliable inventory of all linked detail pages, their URLs, page titles, redirects/status, source tile/category, content/assets and any duplicate or non-migration targets. The pages will then be reviewed and adjusted before migration rather than copied blindly.
- 2026-09-08 — Contentful Migration: Completed the first Codex crawl of the Gold-membership product-detail scope. It found 38 product tiles and 36 unique target pages, with one redirect, two duplicate targets and one broken Objektdatenbank target returning HTTP 404. The crawl exported per-page metadata, complete content/source packages, links and assets; across the scope it recorded 1,723 asset references, downloaded 300 local original files and reported zero download errors. The known limitation is that the local Chrome CLI did not provide a rendered browser DOM or full-page screenshots, so all 36 pages are marked with `RENDERED_DOM_UNAVAILABLE` / `FULLPAGE_SCREENSHOT_UNAVAILABLE`; content that depends on interaction, consent, login or asynchronous JavaScript is therefore not fully verified.
- 2026-09-08 — Contentful Migration / Landing Page Builder: Clarified the working split for the next migration phase. Dominik should not spend substantial time on repeated crawling or manual page migration; operational migration work should move to Ulrike, with Mitch supporting her, while Dominik concentrates on preparing the LP Builder, reusable modules, contracts and quality guardrails that make the migration scalable.
- 2026-09-08 — Landing Page Builder: Started the next module-readiness phase for the B2B migration. The first confirmed candidate is the large B2B product-comparison table used on multiple AEM pages. The existing AEM HTML should serve as the canonical structural reference because the table is complex and its underlying architecture should remain stable. The intended Builder contract separates controlled structure/responsive/interaction behavior from editable content such as product columns, rows, labels, tooltips, links, cell states and CTAs. A Codex implementation prompt was prepared; implementation outcome is not yet confirmed.
- 2026-09-08 — Landing Page Builder: Codex prepared the active local `product-comparison-table` module contract and supporting implementation across the module contracts, dedicated module documentation, component library, bridge CSS, runtime test and open-gap tracking. The Contentful adaptation uses semantic table markup, namespaced `lpb-product-comparison__*` classes, verified COSMA icons, CoreCSS/bridge tokens and native `details/summary` for groups and tooltips, with no legacy `feat-*` classes, inline CSS or script tags. The GPT may change plan, group, feature, tooltip, CTA and cell content but must preserve the table matrix and class structure, with exactly one plan cell per feature row. Remaining `OPEN / VERIFY` items are the sticky feature column, mobile horizontal scrolling and nested native `details` behavior in a real Contentful Preview. The future central runtime is not required for the base module and would only be used later for confirmed focus/dismissal enhancements if needed.
- 2026-09-08 — Landing Page Builder: A structured AEM-vs.-LP-Builder review classified the current `product-comparison-table` as `NEEDS LOCAL FIXES BEFORE PREVIEW`. Confirmed issues include an incorrect Bronze status for `Anbieterbewertung`, an incomplete/inconsistent tariff-header contract compared with AEM, and the fact that the current comparison CSS candidate is not yet delivered through the centrally/publicly loaded bridge, which would make a Contentful Preview non-representative. Additional local cleanup is needed around `profi-plus-module` highlighting and the scope/labeling of tooltip demo coverage. After those fixes, Contentful Preview should validate accordion behavior, horizontal scroll plus sticky feature column, tooltip behavior inside the scroll viewport, CTA/header links and the 668/669px breakpoint.
- 2026-09-08 — Landing Page Builder: Dominik discovered that the AEM reference initially supplied to Codex was incomplete. The live AEM page also references a separate source-code snippet at `/content/campaigns/is24products/snippets/product-table-css-scripts/jcr:content/par/sourcecode`, which may contain CSS and/or JavaScript required for the original comparison-table behavior. The current Codex review and local-fix pass must therefore be treated as provisional until this additional AEM dependency has been captured and compared against the existing module implementation, especially for previously unclear interaction, tooltip, responsive, sticky and highlight behavior.
- 2026-09-08 — Landing Page Builder: The recovered AEM `styles.css` plus JavaScript was reviewed against the current `product-comparison-table`. The full reference confirms that native `details/summary` preserves the required independent multi-open accordion behavior and that native details-based tooltips are a deliberate touch/keyboard improvement over the original hover-only tooltip; no runtime is required for either. It also confirms the AEM responsive contract as horizontal scrolling with `80rem` desktop minimum width and `940px` at `max-width: 668px`; the current local bridge values therefore need correction. Sticky feature-column behavior is a Contentful enhancement rather than AEM fidelity, `feat__highlight` is definitively inactive in the provided AEM CSS, and the repeated per-group tariff header remains a valid semantic Contentful adaptation that must be documented as such. Bridge delivery remains a No-Go until these four local adjustments are applied and revalidated.
