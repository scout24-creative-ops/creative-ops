# Do-not-touch-Regeln

## Legacy Runtime
`is24-lp-creator/lp-creator` nicht löschen, nicht privat machen, nicht umbenennen und GitHub Pages nicht deaktivieren.

Alte CSS- und JS-Pfade können noch von bestehenden AEM-Seiten geladen werden.

## Neue Runtime
Neue Landingpages verwenden nur noch:

`https://scout24-creative-ops.github.io/lp-builder/runtime/...`

Dateien unter `lp-builder/runtime/**` nicht unüberlegt verschieben, löschen oder umbenennen.

## Creative Hub
`DominikBoehme/ai-marketing-creative-hub` bleibt als Redirect bestehen und wird nicht aktiv weiterentwickelt.

## Design Library
Die Live Design Library bleibt vorerst in `s24-creative-ops/design-library`. Eine Migration wird separat geplant.

## Änderungen
Keine versteckten Auto-Commits oder Auto-Pushes. Änderungen an diesem Work-Context erfolgen nur nach Dominiks Freigabe.
